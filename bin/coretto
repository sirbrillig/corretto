#!/usr/bin/env php -q
<?php

require( './vendor/autoload.php' );

array_shift( $argv );
if ( count( $argv ) < 1 ) {
	echo "USAGE: corretto [test_file1.php]...\n";
	die;
}
$loadFile = function( $fileName ) {
	require( $fileName );
};

$root = new \Corretto\RootSuite();
\Corretto\setRootSuite( $root );
array_map( $loadFile, $argv );
new \Corretto\Reporters\Base( $root );
$root->run();
